import { sprintf } from 'sprintf-js';

const msg = sprintf('My name is%sand I am%dyears old', 'Alice', 30);
console.log(msg); // My name is Alice and I am 30 years old
import { randomUUID } from 'node:crypto';
console.log(randomUUID());
